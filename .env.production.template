# ====================================================================================
# AI Chat Studio - 生产环境配置模板
# ====================================================================================
# 重要提示:
# 1. 复制此文件为 .env.production
# 2. 填写真实的配置值（不要使用默认值！）
# 3. 确保 .env.production 在 .gitignore 中，永远不要提交到代码库
# ====================================================================================

# ==================== 安全配置（必须修改！） ====================

# 应用密钥 - 用于 JWT 签名和加密
# 生成命令: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
VITE_APP_SECRET_KEY=CHANGE_THIS_TO_A_SECURE_RANDOM_KEY_AT_LEAST_32_CHARACTERS

# JWT 密钥（与后端保持一致）
VITE_JWT_SECRET=CHANGE_THIS_TO_A_SECURE_JWT_SECRET

# API 加密密钥（用于加密存储的 API keys）
VITE_ENCRYPTION_KEY=CHANGE_THIS_TO_A_32_CHARACTER_ENCRYPTION_KEY

# ==================== API 配置 ====================

# OpenAI API 配置
VITE_OPENAI_API_KEY=
VITE_OPENAI_ORG_ID=
VITE_OPENAI_API_BASE=https://api.openai.com/v1

# Anthropic Claude API
VITE_ANTHROPIC_API_KEY=
VITE_ANTHROPIC_API_BASE=https://api.anthropic.com

# Google Gemini API
VITE_GOOGLE_API_KEY=
VITE_GOOGLE_API_BASE=https://generativelanguage.googleapis.com/v1beta

# 自定义 API 端点
VITE_CUSTOM_API_BASE=

# ==================== 后端服务配置 ====================

# 后端 API 地址（生产环境）
VITE_API_BASE_URL=https://api.yourdomain.com
VITE_WS_URL=wss://api.yourdomain.com/ws

# 或使用自托管后端
# VITE_API_BASE_URL=https://your-backend.herokuapp.com
# VITE_WS_URL=wss://your-backend.herokuapp.com/ws

# ==================== 功能开关 ====================

# 启用的功能
VITE_ENABLE_VOICE_CHAT=true
VITE_ENABLE_CODE_EXECUTION=false  # 生产环境建议关闭
VITE_ENABLE_FILE_UPLOAD=true
VITE_ENABLE_IMAGE_GENERATION=true
VITE_ENABLE_WEB_SEARCH=true
VITE_ENABLE_PLUGINS=true
VITE_ENABLE_ANALYTICS=true

# PWA 配置
VITE_ENABLE_PWA=true
VITE_PWA_CACHE_STRATEGY=network-first

# ==================== 安全设置 ====================

# CORS 配置（仅允许特定域名）
VITE_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# 内容安全策略
VITE_ENABLE_CSP=true
VITE_CSP_REPORT_URI=https://yourdomain.com/api/csp-report

# 安全头配置
VITE_ENABLE_HSTS=true
VITE_ENABLE_XSS_PROTECTION=true
VITE_ENABLE_FRAME_PROTECTION=true

# ==================== 性能配置 ====================

# 缓存设置
VITE_ENABLE_CACHE=true
VITE_CACHE_TTL=3600  # 1 hour in seconds
VITE_MAX_CACHE_SIZE=104857600  # 100MB in bytes

# 请求限制
VITE_MAX_REQUEST_SIZE=10485760  # 10MB in bytes
VITE_REQUEST_TIMEOUT=30000  # 30 seconds in milliseconds

# 消息限制
VITE_MAX_MESSAGE_LENGTH=8000
VITE_MAX_CONVERSATION_MESSAGES=100

# ==================== 日志和监控 ====================

# 日志级别 (error, warn, info, debug)
VITE_LOG_LEVEL=warn

# 错误追踪 (Sentry)
VITE_SENTRY_DSN=
VITE_SENTRY_ENVIRONMENT=production
VITE_SENTRY_TRACES_SAMPLE_RATE=0.1

# Google Analytics
VITE_GA_MEASUREMENT_ID=

# 性能监控
VITE_ENABLE_PERFORMANCE_MONITORING=true

# ==================== 数据库配置（后端） ====================

# PostgreSQL
DATABASE_URL=postgresql://username:password@localhost:5432/chat_studio_prod

# Redis
REDIS_URL=redis://:password@localhost:6379/0

# ==================== 存储配置 ====================

# 本地存储限制
VITE_LOCAL_STORAGE_QUOTA=52428800  # 50MB in bytes

# 文件上传配置
VITE_MAX_FILE_SIZE=10485760  # 10MB in bytes
VITE_ALLOWED_FILE_TYPES=.txt,.pdf,.doc,.docx,.png,.jpg,.jpeg

# ==================== 第三方服务 ====================

# Stripe (如果需要支付功能)
VITE_STRIPE_PUBLIC_KEY=

# AWS S3 (如果需要云存储)
VITE_AWS_REGION=
VITE_AWS_S3_BUCKET=

# ==================== 应用配置 ====================

# 应用信息
VITE_APP_NAME=AI Chat Studio
VITE_APP_VERSION=2.1.0
VITE_APP_ENV=production

# 调试模式（生产环境必须设为 false）
VITE_DEBUG_MODE=false

# 显示 API 文档（生产环境建议关闭）
VITE_SHOW_API_DOCS=false

# ==================== 速率限制 ====================

# API 调用限制
VITE_RATE_LIMIT_PER_MINUTE=60
VITE_RATE_LIMIT_PER_HOUR=1000
VITE_RATE_LIMIT_PER_DAY=10000

# ==================== 通知配置 ====================

# Email 通知
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@yourdomain.com

# Push 通知
VITE_VAPID_PUBLIC_KEY=

# ==================== 备份配置 ====================

# 自动备份
VITE_ENABLE_AUTO_BACKUP=true
VITE_BACKUP_INTERVAL=86400000  # 24 hours in milliseconds
VITE_MAX_BACKUPS=7

# ==================== 国际化 ====================

# 默认语言
VITE_DEFAULT_LANGUAGE=zh-CN
VITE_SUPPORTED_LANGUAGES=zh-CN,en-US,ja-JP

# ==================== 部署信息 ====================

# 部署时间戳（自动生成）
VITE_BUILD_TIME=
VITE_BUILD_COMMIT=
VITE_BUILD_BRANCH=

# ====================================================================================
# 安全检查清单:
# ====================================================================================
# [ ] 已修改所有 "CHANGE_THIS_*" 开头的密钥
# [ ] SECRET_KEY 至少 32 个字符
# [ ] JWT_SECRET 使用随机生成的强密钥
# [ ] ENCRYPTION_KEY 正好 32 个字符
# [ ] DEBUG_MODE 设为 false
# [ ] LOG_LEVEL 设为 warn 或 error
# [ ] ALLOWED_ORIGINS 只包含实际域名
# [ ] 所有 API keys 已正确配置
# [ ] 数据库连接字符串使用强密码
# [ ] .env.production 已添加到 .gitignore
# [ ] 敏感信息未硬编码在代码中
# ====================================================================================
