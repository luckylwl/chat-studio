# ================================
# AI Chat Studio Backend - 环境变量配置示例
# ================================
# 复制此文件为 .env 并填入你的实际值
# 重要提示: 永远不要将 .env 文件提交到代码库！

# ================================
# 应用配置
# ================================
ENVIRONMENT=development
HOST=0.0.0.0
PORT=8000
RELOAD=true
LOG_LEVEL=info

# ================================
# 安全配置（必须修改！）
# ================================
# JWT密钥 - 用于生成和验证JWT令牌
# 生成命令: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-super-secret-key-change-this-in-production
JWT_SECRET=your-jwt-secret-key-change-this-in-production

# Token过期时间（分钟）
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ================================
# 数据库配置
# ================================
# PostgreSQL连接字符串
# 格式: postgresql://用户名:密码@主机:端口/数据库名
DATABASE_URL=postgresql://postgres:your-secure-password@localhost:5432/chat_studio

# 用于 docker-compose
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-secure-password
POSTGRES_DB=chat_studio

# SQL调试输出
SQL_ECHO=false

# ================================
# Redis缓存配置
# ================================
REDIS_URL=redis://localhost:6379/0

# ================================
# CORS配置
# ================================
# 允许的来源（逗号分隔）
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# ================================
# AI API 配置（可选）
# ================================
# 如果后端需要直接调用AI API
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=

# ================================
# 文件上传配置
# ================================
MAX_UPLOAD_SIZE=10485760  # 10MB in bytes
UPLOAD_DIR=./uploads

# ================================
# 监控配置（可选）
# ================================
SENTRY_DSN=
SENTRY_ENVIRONMENT=development

# ================================
# 邮件配置（可选）
# ================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@yourdomain.com

# ================================
# 测试配置
# ================================
# 测试环境的后端URL
BACKEND_URL=http://localhost:8000

# ================================
# 安全检查清单
# ================================
# [ ] SECRET_KEY 已修改为强随机密钥
# [ ] JWT_SECRET 已修改为强随机密钥
# [ ] 数据库密码已修改为强密码
# [ ] ALLOWED_ORIGINS 只包含实际需要的域名
# [ ] .env 文件已添加到 .gitignore
# [ ] 生产环境的敏感信息未硬编码在代码中
